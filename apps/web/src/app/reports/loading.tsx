import { CartLoader } from '@/components/ui/cart-loader';

export default function ReportsLoading() {
  return <CartLoader fullScreen title="Loading Reports" description="Crunching your numbers..." />;
}
